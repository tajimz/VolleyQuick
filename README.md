
### 🚀 Getting Started with VolleyQuick

**1️⃣ Add the dependency**
In your module-level `build.gradle` (usually `app/build.gradle`), add:

```gradle
implementation 'com.github.tajimz:volleyquick:1.2.1'
```

**2️⃣ Add JitPack repository**
In your **root** `settings.gradle`, add the following at the end of the `repositories` block:

```gradle
maven { url 'https://jitpack.io' }
```

**3️⃣ Done!**
No additional setup required. You can now use VolleyQuick in your project.
Check the reference code for usage examples.

---
